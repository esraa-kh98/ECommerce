@model string;
 @if(Context.User.Identity.IsAuthenticated && User.IsInRole("Admin"))
 {
<div class="text-white" style="position:fixed;right:30px;bottom:100px;">
    <a class="btn btn-outline-info" asp-action="Create" asp-controller="@Model">
        <i class="fas fa-regular fa-plus-square mr-3"></i>
        Create</a>
</div>
 }